package com.justin.cipher;

import java.util.Map;

public interface Cipherable {
	public String useCipher(Map<Character, Character> charsMap, char[] charArray);
	public String useDeCipher(Map<Character, Character> charsMap, char[] charArray);
}
